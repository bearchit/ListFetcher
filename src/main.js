/* ListFetcher main */

// Base function.
var ListFetcher = function() {
  // Add functionality here.
  return true;
};


// Version.
ListFetcher.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.ListFetcher = ListFetcher;
